import './globals.css';

export const metadata = {
  title: 'Generator Foto ATK — SMP Negeri 1 Poli-Polia',
  description: 'Generator dokumentasi foto ATK dengan ukuran relatif, jumlah presisi, perspektif meja, dan GPS/timestamp.'
};

export default function RootLayout({ children }) {
  return <html lang="id"><body>{children}</body></html>;
}
